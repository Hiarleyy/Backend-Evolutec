function serve(_req, _res, next) {
  next()
}

function setup(spec) {
  return function swaggerUiMiddleware(_req, res) {
    res.status(200).json({
      message: "Swagger UI placeholder",
      spec
    })
  }
}

module.exports = {
  serve,
  setup
}
